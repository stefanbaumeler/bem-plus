export type TSeparators = {
    element: string;
    modifier: string;
    mixinElement: string;
};
export declare enum EOutputMode {
    absolute = "absolute",
    relative = "relative"
}
export declare enum EOutputLanguage {
    js = "js",
    ts = "ts"
}
export declare enum EStrategy {
    dist = "dist",
    plus = "plus"
}
